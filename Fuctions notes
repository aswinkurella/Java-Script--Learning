Functions:
#Functions is s imply a block of code which can be invoked or executed as many times as we want in different places.
we can say as reuasable code

syntax:
function(){
--code}
we can even pass by parametrs

Scope:
We can access even if we call function first and then function also itr will execute and give answer.

Lets say if we use same variable inside and outside function but once it executes function the variable value wont change as outside value which is bit
diffferent compared to other lanaguages.

-->If we dont wnat to change value outside then we need to create exactly a local variable which is called parameter.
-->Local variabvles will execute only inside function post complete function it will be dleeted which amkes memory free .

**It is not recommended to declare var inside function .

Difference between let and var:

let ranges block.
var range is functions.
let :
1)Range(to block)
2)mememory release(immediatley after block)
3)Hoisting(None)
4)Redeclaration(secure)

var:
1)Range(function)
2)mememory release(after leaving function)
3)Hoisting(yes)
4)Redeclaration(declare as much as you want)

***If we want to reuse make it var inside loop otherwise keep it as let
